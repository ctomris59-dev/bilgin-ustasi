export default function MistakeBox({ profile, onStartRetryTest }) {
  const active=(profile.mistakeBox||[]).filter((m)=>!m.resolved); const bySubject=active.reduce((acc,m)=>{(acc[m.subject] ||= []).push(m);return acc;},{});
  return <div className="app-shell space-y-4 pb-5"><section className="glass-card relative overflow-hidden p-5"><div className="pointer-events-none absolute -right-16 -top-16 h-40 w-40 rounded-full bg-[#FF789E]/10 blur-3xl"/><div className="relative z-10 flex items-start gap-3"><div className="flex h-12 w-12 items-center justify-center rounded-2xl bg-[#FF789E]/10 text-xl text-[#FF789E]">↻</div><div><p className="text-[9px] font-black uppercase tracking-[.2em] text-[#FF789E]">Öğrenme Döngüsü</p><h2 className="font-display mt-1 text-xl font-black">Tekrar Merkezi</h2><p className="mt-2 text-xs leading-relaxed text-[#8793B4]">Zorlandığın sorular burada tutulur. Rövanş görevleri, bilgiyi yeniden hatırlayıp kalıcı hale getirmen için tasarlandı.</p></div></div></section>
    {active.length===0?<div className="glass-card p-7 text-center"><div className="mx-auto flex h-12 w-12 items-center justify-center rounded-2xl bg-[#52E3C2]/10 text-[#52E3C2]">✓</div><p className="mt-3 text-sm font-black">Şu an bekleyen tekrar yok</p><p className="mt-1 text-xs text-[#8793B4]">Yeni bir görevde zorlandığın soru olursa burada görünür.</p></div>:<><div className="grid grid-cols-2 gap-2.5"><Mini value={active.length} label="Bekleyen soru" color="#FF789E"/><Mini value={Object.keys(bySubject).length} label="Ders" color="#A98CFF"/></div>{Object.entries(bySubject).map(([subject,items])=><section key={subject} className="glass-card p-4"><div className="flex items-center justify-between"><div><p className="text-sm font-black">{subject}</p><p className="mt-1 text-[10px] text-[#8793B4]">{items.length} soru yeniden çalışılacak</p></div><span className="game-chip">{items.length}</span></div><div className="mt-3 h-1.5 overflow-hidden rounded-full bg-white/[.06]"><div className="h-full rounded-full bg-gradient-to-r from-[#FF789E] to-[#A98CFF]" style={{width:`${Math.min(100,30+items.length*8)}%`}}/></div><button onClick={()=>onStartRetryTest(subject,items)} className="sticker-btn mt-4 w-full py-3 text-xs">Rövanş Görevini Başlat →</button></section>)}</>}
  </div>;
}
function Mini({value,label,color}){return <div className="glass-card p-3.5"><p className="font-display text-xl font-black" style={{color}}>{value}</p><p className="mt-1 text-[9px] font-black uppercase tracking-[.14em] text-[#8793B4]">{label}</p></div>}
